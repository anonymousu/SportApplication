server.port=8082

spring.datasource.url = jdbc:mysql://localhost:3306/sports?allowPublicKeyRetrieval=true&useSSL=false
#jdbc:mysql://<hostname>:<port>/<dbname>?prop1
spring.datasource.username = root
spring.datasource.password = prerna


## Hibernate Properties
# The SQL dialect makes Hibernate generate better SQL for the chosen database
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQL5InnoDBDialect
spring.jpa.show-sql=true

# Hibernate ddl auto (create, create-drop, validate, update)
spring.jpa.hibernate.ddl-auto = update

## MULTIPART (MultipartProperties)
# Enable multipart uploads
spring.servlet.multipart.enabled=true
# Threshold after which files are written to disk.
spring.servlet.multipart.file-size-threshold=2GB
# Max file size.
spring.servlet.multipart.max-file-size=1024GB
# Max Request Size
spring.servlet.multipart.max-request-size=1024GB

#server.servlet.context-path=/sports-crud-rest
